# Simple CRUD Program with Pyhton & MySQL

Simple CRUD (Create Read Update Delete) Program with python
## Installation

Install packages with pip

```bash
  pip install mysql-connector 
```
And Import curdpy.sql into a mysql
